---
phase: quick-260615-q7f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/daily-targets.ts
  - tests/daily-targets.test.ts
  - app/(protected)/queue/actions.ts
  - app/(protected)/queue/page.tsx
autonomous: true
requirements: [Q7F-DYNAMIC-TARGETS]

must_haves:
  truths:
    - "Manager sees «План дня» revenue derived from their own sales_plans.repeat_target ÷ calendar weekdays of the current month — no hardcoded 85К/5/22/17000 in the path"
    - "Admin sees an «Отдел» badge with department-sum targets (Σ repeat_target ÷ weekdays) and today's facts counted across ALL managers"
    - "callsPerDay for admin = day_target × number of active managers; for manager = day_target"
    - "A manager with no plan row (admin, or new manager before a plan is added) shows «план не задан», NOT a substituted 85К/5"
    - "Adding a sales_plans row for a new manager raises the department revenue target automatically on next 30s poll; no caching"
    - "No division-by-zero in any progress bar (calls / orders / revenue)"
  artifacts:
    - path: "lib/daily-targets.ts"
      provides: "workingWeekdaysInMonth + deriveDailyTargets pure helpers"
      exports: ["workingWeekdaysInMonth", "deriveDailyTargets"]
    - path: "tests/daily-targets.test.ts"
      provides: "Pure-logic vitest coverage of both helpers"
    - path: "app/(protected)/queue/actions.ts"
      provides: "getDayStats with scope-aware targets + facts via helpers"
    - path: "app/(protected)/queue/page.tsx"
      provides: "scope-aware «План дня» widget with «план не задан» / «Отдел» states"
  key_links:
    - from: "app/(protected)/queue/actions.ts"
      to: "lib/daily-targets.ts"
      via: "import deriveDailyTargets, workingWeekdaysInMonth"
      pattern: "from '@/lib/daily-targets'"
    - from: "app/(protected)/queue/actions.ts"
      to: "getUserRole"
      via: "isAdmin = getUserRole(user) === 'admin'"
      pattern: "getUserRole\\(user\\)"
---

<objective>
Make the queue «План дня» header targets fully dynamic from `public.sales_plans` (per-manager monthly repeat_target) and `crm_settings` (avg_check, day_target). Remove all hardcoded business numbers (85000, 5, 22, 17000) from the active path. Manager scope = personal plan; admin scope = department aggregate over ALL managers, with facts counted across everyone. Working days come from the real calendar, not a constant. Adding/removing a manager or editing any plan reflects automatically on the next 30s poll.

Purpose: A manager's header must mirror their actual monthly plan; the owner must see a true department-wide daily target and today's department-wide progress — both without manual constants.
Output: 1 new pure-logic lib + its test, 1 edited server action, 1 edited client widget.
</objective>

<context>
@.planning/STATE.md

<interfaces>
<!-- Verified from codebase this session — use directly, no exploration. -->

From lib/auth/get-user-role.ts (pure, safe in server actions; NOT yet imported in queue/actions.ts):
```typescript
export function getUserRole(user: User | null): 'admin' | 'manager' | null
```

From types/database.ts:
```
sales_plans.Row: { manager_id, month, year, repeat_target: number, ... } // *_target whole TENGE
profiles.Row:    { id, role: string, is_active: boolean | null, ... }      // active = role <> 'admin' AND is_active !== false
crm_settings.Row:{ key: string, value: Json }                              // day_target = jsonb number; sales_plan = jsonb { avg_check, ... }
orders.Row:      { manager_id, amount: number, created_at, ... }
call_logs.Row:   { manager_id, status, sub_status, created_at, ... }
```

Current getDayStats response fields (PRESERVE every name; ADD only `scope`):
```typescript
{ calls, reached, orders, revenue, whatsapp, planRevenuePerDay, planOrdersPerDay, dayTargetCalls }
```

avg_check / day_target defaults already centralized in settings (defaultPlan.avg_check = 17000, dayTarget = 40) — mirror as LAST-RESORT only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure daily-targets helpers + vitest</name>
  <files>lib/daily-targets.ts, tests/daily-targets.test.ts</files>
  <read_first>
    - tests/order-discount.test.ts (vitest style to mirror: describe/it/expect, Russian test names)
    - tests/motivation-formula.test.ts (pure-logic test style)
  </read_first>
  <behavior>
    - workingWeekdaysInMonth(2026, 6): counts Mon–Fri in June 2026 (June 2026 = 22 weekdays); always returns >= 1.
    - workingWeekdaysInMonth handles month boundaries correctly (use new Date(year, month1to12-1, day) day-loop, getDay() in [1..5]).
    - deriveDailyTargets normal manager: { repeatPlanTenge: 1895603, avgCheck: 17000, callsTarget: 40, workingDays: 22, managerCount: 1 }
      → revenuePerDay = Math.round(1895603/22) = 86164; ordersPerDay = Math.max(1, Math.round(86164/17000)) = 5; callsPerDay = 40.
    - deriveDailyTargets zero/absent plan: repeatPlanTenge=0 → revenuePerDay=0, ordersPerDay=0 (NOT 85000/5); callsPerDay still = callsTarget*max(1,managerCount).
    - deriveDailyTargets department managerCount=2: callsPerDay = callsTarget*2 (e.g. 40*2=80); revenuePerDay scales with summed plan.
    - deriveDailyTargets avgCheck=0: does NOT crash → ordersPerDay=0; revenuePerDay still computed from plan.
    - No division by zero: workingDays<=0 floored to 1 inside revenuePerDay; managerCount<1 floored to 1 inside callsPerDay.
  </behavior>
  <action>
    Create `lib/daily-targets.ts` with two exported pure functions (no imports, no `any`, explicit return types, each ≤30 lines):

    `export function workingWeekdaysInMonth(year: number, month1to12: number): number` — iterate days 1..daysInMonth (`new Date(year, month1to12, 0).getDate()`), count days where `new Date(year, month1to12 - 1, day).getDay()` is 1..5, return `Math.max(1, count)`.

    `export function deriveDailyTargets(input: { repeatPlanTenge: number; avgCheck: number; callsTarget: number; workingDays: number; managerCount: number }): { revenuePerDay: number; ordersPerDay: number; callsPerDay: number }` — compute exactly per &lt;behavior&gt;:
    - `const days = Math.max(1, input.workingDays)`
    - `const callsPerDay = input.callsTarget * Math.max(1, input.managerCount)`
    - if `input.repeatPlanTenge <= 0` → `{ revenuePerDay: 0, ordersPerDay: 0, callsPerDay }`
    - else `const revenuePerDay = Math.round(input.repeatPlanTenge / days)`; `const ordersPerDay = input.avgCheck > 0 ? Math.max(1, Math.round(revenuePerDay / input.avgCheck)) : 0`; return all three.

    Create `tests/daily-targets.test.ts` mirroring tests/order-discount.test.ts (no DB mock needed — pure functions). Import from `@/lib/daily-targets`. Cover every &lt;behavior&gt; bullet. For the June-2026 weekday assertion, assert the exact computed count (22) rather than a magic guess — derive expectation in the test from a comment.
  </action>
  <verify>
    <automated>cd "D:/Mind map/Dara Clean/crm" && npm run test -- daily-targets 2>&amp;1 | tail -20</automated>
  </verify>
  <done>tests/daily-targets.test.ts passes; both functions exported with explicit types; no `any`, no magic 85000/5/22/17000 baked into the lib.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite getDayStats to be scope-aware via helpers</name>
  <files>app/(protected)/queue/actions.ts</files>
  <read_first>
    - app/(protected)/queue/actions.ts lines 364–487 (almatyTodayUtc, WHATSAPP_SUB_STATUS, getDayStats)
    - app/(protected)/settings/actions.ts lines 50, 71–72 (defaultPlan.avg_check=17000, dayTarget=40 — mirror as last-resort)
    - lib/daily-targets.ts (created in Task 1)
  </read_first>
  <action>
    Add import: `import { getUserRole } from '@/lib/auth/get-user-role'` and `import { deriveDailyTargets, workingWeekdaysInMonth } from '@/lib/daily-targets'` at top of file (getUserRole is NOT currently imported here — add it).

    Refactor `getDayStats()` so each function is ≤30 lines. Extract two private async helpers in the same file:

    `getTodayFacts(supabase, { managerId }: { managerId: string | null }, todayUtc: string)` — runs the existing 4 count queries + revenue query, but when `managerId === null` (admin/department) OMIT every `.eq('manager_id', ...)` so facts span ALL managers; otherwise apply `.eq('manager_id', managerId)`. PRESERVE the WhatsApp exclusion: calls count keeps `.or('sub_status.is.null,sub_status.neq.' + WHATSAPP_SUB_STATUS)`, reached keeps the same `.or` + `.eq('status','reached')`, whatsapp keeps `.eq('sub_status', WHATSAPP_SUB_STATUS)`. Returns `{ calls, reached, orders, revenue, whatsapp }` (numbers). Use the typed client — no `select('*')`, no `as any`.

    `getDailyTargets(supabase, { isAdmin, userId, month, year }: { isAdmin: boolean; userId: string; month: number; year: number })` — single crm_settings read `.select('key, value').in('key', ['day_target', 'sales_plan'])`; parse `avgCheck` from the `sales_plan` row's `value.avg_check` (typeof-number guard, else 17000) and `callsTarget` from the `day_target` row value (typeof-number guard, else 40). Compute `const workingDays = workingWeekdaysInMonth(year, month)`. Then:
    - personal (isAdmin false): read the user's own row `.from('sales_plans').select('repeat_target').eq('manager_id', userId).eq('month', month).eq('year', year).maybeSingle()`; `repeatPlanTenge = Number(row?.repeat_target) || 0`; `managerCount = 1`.
    - department (isAdmin true): read ALL rows `.from('sales_plans').select('repeat_target').eq('month', month).eq('year', year)`, sum `Number(r.repeat_target) || 0`; count active managers via `.from('profiles').select('role, is_active')` then `managerCount = rows.filter(p => p.role !== 'admin' && p.is_active !== false).length` (floor to 1 happens inside deriveDailyTargets).
    Call `deriveDailyTargets({ repeatPlanTenge, avgCheck, callsTarget, workingDays, managerCount })`; return its result. Keep each branch tight; if either branch grows past 30 lines, split the personal/department plan-read into a third tiny helper.

    Rewrite `getDayStats()` body: get user; if `!user` early-return the existing zero/last-resort object PLUS `scope: 'personal' as const` (keep current 85000/5/40 ONLY in this unauthenticated guard). Else compute `const isAdmin = getUserRole(user) === 'admin'`; `const scope = isAdmin ? 'department' as const : 'personal' as const`; `const month = now.getMonth() + 1`, `const year = now.getFullYear()`; run `getTodayFacts(supabase, { managerId: isAdmin ? null : user.id }, almatyTodayUtc())` and `getDailyTargets(supabase, { isAdmin, userId: user.id, month, year })` (Promise.all is fine). Return ALL existing field names mapped from facts + targets: `{ calls, reached, orders, revenue, whatsapp, planRevenuePerDay: targets.revenuePerDay, planOrdersPerDay: targets.ordersPerDay, dayTargetCalls: targets.callsPerDay, scope }`.

    Remove the old inline 6-category sum, `/22`, `/17000`, and the standalone day_target read — they are replaced by the helpers. Do NOT touch any other export in the file.
  </action>
  <verify>
    <automated>cd "D:/Mind map/Dara Clean/crm" && npm run build 2>&amp;1 | tail -25</automated>
  </verify>
  <done>build passes 0 errors; getDayStats returns the 8 original fields + `scope`; no 85000/5/22/17000 outside the `!user` guard; WhatsApp exclusion intact; admin path has no `manager_id` filter on facts; each function ≤30 lines; no `as any`/`select('*')`.</done>
</task>

<task type="auto">
  <name>Task 3: Scope-aware «План дня» widget states</name>
  <files>app/(protected)/queue/page.tsx</files>
  <read_first>
    - app/(protected)/queue/page.tsx lines 89–98 (DayStats type), 143–146 (default seed), 499–534 (widget render)
  </read_first>
  <action>
    Add `scope: 'personal' | 'department'` to the local `DayStats` type (after `dayTargetCalls`) and to the default state seed at lines 143–146 (`scope: 'personal'`).

    In the «План дня» widget (lines 502–533):
    - Render a small «Отдел» badge inside the widget container when `stats.scope === 'department'` (reuse the existing `<Badge variant="outline">` styling pattern; place it before the «Звонки» block or at the row start).
    - «Заказы» block: when `stats.planOrdersPerDay === 0`, render the target side as «план не задан» (or «—») instead of `/{stats.planOrdersPerDay}` and DO NOT render the orders progress bar. Otherwise render as today, but guard the width: `Math.min(stats.orders / stats.planOrdersPerDay * 100, 100)` is only reached when planOrdersPerDay > 0 (the conditional already guarantees it).
    - «Выручка» block: when `stats.planRevenuePerDay === 0`, show «план не задан» (or «—») for the target side instead of `/{(stats.planRevenuePerDay/1000).toFixed(0)}К ₸`. (Revenue block currently has no progress bar — keep it that way; just guard the displayed target.)
    - «Звонки» block: guard the progress bar against `dayTargetCalls < 1` — only render the bar / divisor when `stats.dayTargetCalls >= 1`; if `< 1` show the count without `/N` and without the bar (avoids `/0`).
    Keep the conversion Badge (lines 528–532) and all other widget content unchanged. No `as any`, no inline business numbers.
  </action>
  <verify>
    <automated>cd "D:/Mind map/Dara Clean/crm" && npm run build 2>&amp;1 | tail -15</automated>
  </verify>
  <done>build passes; widget shows «Отдел» badge for admin; shows «план не задан» (no bar) when planOrders/planRevenue = 0; no division-by-zero in any progress-bar width; manager with a real June 2026 plan shows revenue ≈86–95К, orders ≈5, calls 40.</done>
</task>

</tasks>

<verification>
- `npm run test` green (daily-targets.test.ts + existing suites unaffected).
- `npm run build` 0 errors.
- Manual (DevTools MCP / browser, post-build): log in as manager Елена/Самал → header revenue ≈ June repeat (1.9M) ÷ June weekdays ≈ 86–95К/day, orders ≈5, calls 40, no «Отдел» badge. Log in as admin → «Отдел» badge, department-sum revenue target, calls = 40×activeManagers, facts (calls/orders/revenue/whatsapp) summed across all managers.
- Add a sales_plans row for a third manager on /sales-plans → admin department target rises on next 30s poll.
- Inspect progress bars: no element gets `width: NaN%`/`Infinity%` when a plan/target is 0.
</verification>

<success_criteria>
- No hardcoded 85000 / 5 / 22 / 17000 anywhere in the active getDayStats path (only the `!user` unauthenticated guard and last-resort settings defaults retain numbers).
- `deriveDailyTargets` + `workingWeekdaysInMonth` are pure, tested, and the single source of the target math.
- Admin facts span ALL managers (no `manager_id` filter); WhatsApp exclusion preserved in calls count.
- «план не задан» shown (no bar) when a plan target is 0; no division-by-zero in any bar.
- motivation/* and dashboard/* (and their local WORKING_DAYS_PER_MONTH=22) untouched; queue filtering/locks/auto-select unchanged; getDayStats field names preserved (+`scope`).
</success_criteria>

<output>
Create `.planning/quick/260615-q7f-queue-daily-targets-from-plan/260615-q7f-SUMMARY.md` when done.
</output>
