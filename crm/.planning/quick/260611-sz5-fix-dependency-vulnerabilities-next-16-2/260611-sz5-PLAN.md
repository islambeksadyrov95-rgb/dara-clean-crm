---
phase: quick-260611-sz5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [SEC-DEP-01]
must_haves:
  truths:
    - "next is bumped to a patched version (16.2.9), no MEDIUM vuln from next remains"
    - "xlsx resolves to SheetJS CDN 0.20.3 (>= 0.20.2), both HIGH CVEs fixed"
    - "npm install completes against the updated package.json"
    - "npm run build passes with 0 errors — all three xlsx callers compile unchanged"
    - "npm audit reports no HIGH severity advisory for xlsx"
  artifacts:
    - path: "package.json"
      provides: "Pinned patched dependency versions"
      contains: "cdn.sheetjs.com/xlsx-0.20.3"
    - path: "package-lock.json"
      provides: "Locked resolution tree for the new versions"
  key_links:
    - from: "package.json"
      to: "node_modules/xlsx"
      via: "npm install resolving CDN tarball"
      pattern: "cdn\\.sheetjs\\.com/xlsx-0\\.20\\.3"
    - from: "lib/motivation-excel.ts"
      to: "xlsx 0.20.3"
      via: "import * as XLSX from 'xlsx'"
      pattern: "import \\* as XLSX from 'xlsx'"
---

<objective>
Fix the dependency vulnerabilities flagged by Red Hat Dependency Analytics in `crm/package.json`:
- `next@16.2.6` — 1 known vulnerability (MEDIUM) → bump to patched `16.2.9`
- `xlsx@^0.18.5` — 2 known vulnerabilities (HIGH): prototype pollution CVE-2023-30533 + ReDoS CVE-2024-22363 → move to SheetJS CDN `0.20.3` (npm registry stops at 0.18.5 and will never publish a fix)

Purpose: Authorized defensive hardening of our own production CRM. Removes known HIGH/MEDIUM CVEs from the dependency tree before deployment.

Output: Updated `package.json` + regenerated `package-lock.json`, verified by a clean install, a passing build, and an `npm audit` clean of the xlsx HIGH advisories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- The three xlsx consumers. All use the namespace import, which is API-compatible -->
<!-- between 0.18.5 and 0.20.3. No code changes expected — the build verifies this. -->

lib/motivation-excel.ts:1        → import * as XLSX from 'xlsx'
app/(protected)/import/page.tsx:6 → import * as XLSX from 'xlsx'
app/(protected)/sales-plans/actions.ts:5 → import * as XLSX from 'xlsx'
</interfaces>

<pre_verified_facts>
- Package manager: npm (package-lock.json present). Node v24.
- `next` latest stable = 16.2.9 (verified via `npm view next dist-tags`, 2026-06-11). Bump BOTH `next` (dependencies) and `eslint-config-next` (devDependencies) to 16.2.9 — they must stay in lockstep.
- xlsx fix is NOT on npm. Official SheetJS tarball: https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz (verified HTTP 200; 0.20.4+ return 404). 0.20.3 ≥ 0.20.2 → fixes CVE-2023-30533 and CVE-2024-22363.
- WARNING — dirty working tree: package-lock.json and other files are already modified from unrelated work. Commit ONLY package.json + package-lock.json changes from this bump. Do NOT `git add .` or stage unrelated files.
</pre_verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump next/eslint-config-next to 16.2.9 and pin xlsx to SheetJS CDN 0.20.3</name>
  <files>package.json</files>
  <action>
Edit `package.json` (project root D:\Mind map\Dara Clean\crm) with exactly three string changes — do not touch any other dependency:
1. dependencies → `"next": "16.2.6"` becomes `"next": "16.2.9"` (per the verified latest stable).
2. devDependencies → `"eslint-config-next": "16.2.6"` becomes `"eslint-config-next": "16.2.9"` (must match next's version exactly — eslint-config-next is versioned in lockstep with next).
3. dependencies → `"xlsx": "^0.18.5"` becomes `"xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"` (the npm registry has no patched build; the SheetJS CDN tarball is the canonical fix). Use the literal URL string as the version specifier — this is the SheetJS-documented install method.

Do not edit any other line. Do not reorder keys. Do not change unrelated version ranges.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok = p.dependencies.next==='16.2.9' && p.devDependencies['eslint-config-next']==='16.2.9' && p.dependencies.xlsx==='https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz'; if(!ok){console.error('package.json not updated correctly', JSON.stringify({next:p.dependencies.next, eslint:p.devDependencies['eslint-config-next'], xlsx:p.dependencies.xlsx})); process.exit(1)} console.log('package.json OK')"</automated>
  </verify>
  <done>package.json has next=16.2.9, eslint-config-next=16.2.9, and xlsx pointing at the cdn.sheetjs.com/xlsx-0.20.3 tarball. No other dependency lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Reinstall, regenerate lockfile, and verify build + audit clean</name>
  <files>package-lock.json</files>
  <action>
Run `npm install` in the project root to resolve the new versions and regenerate `package-lock.json`. The xlsx entry in the lockfile must now resolve to the cdn.sheetjs.com tarball (npm stores remote tarball URLs as the `resolved` field).

Then verify the full chain:
1. `npm run build` — must complete with 0 errors. This proves all three xlsx callers (lib/motivation-excel.ts, app/(protected)/import/page.tsx, app/(protected)/sales-plans/actions.ts) compile unchanged against 0.20.3, and that next 16.2.9 builds the app. If a caller fails to compile, the API surface used (XLSX.read / XLSX.utils.* / XLSX.writeFile etc.) differs — STOP and report; do not patch callers without confirming the exact API change against the SheetJS docs.
2. `npm audit` — confirm there is no HIGH severity advisory attributed to `xlsx`. (Other advisories from unrelated packages may remain and are out of scope for this task.)

If `npm install` cannot reach cdn.sheetjs.com (network restriction), STOP and report — the tarball URL is the only fix path; do not silently fall back to the vulnerable npm 0.18.5.
  </action>
  <verify>
    <automated>cd "D:/Mind map/Dara Clean/crm" && npm install && npm run build && node -e "const fs=require('fs'); const lock=fs.readFileSync('package-lock.json','utf8'); if(!lock.includes('cdn.sheetjs.com/xlsx-0.20.3')){console.error('lockfile does not reference xlsx-0.20.3 CDN tarball'); process.exit(1)} console.log('lockfile OK')" && (npm audit --audit-level=high --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{let j;try{j=JSON.parse(s)}catch(e){console.log('audit json unparseable, manual check needed');process.exit(0)} const advs=j.vulnerabilities||{}; const xlsxHigh = advs.xlsx && ['high','critical'].includes(advs.xlsx.severity); if(xlsxHigh){console.error('xlsx still HIGH/CRITICAL in audit');process.exit(1)} console.log('npm audit: no HIGH xlsx advisory')})")</automated>
  </verify>
  <done>npm install succeeds resolving xlsx from the SheetJS CDN tarball; package-lock.json references xlsx-0.20.3; npm run build passes with 0 errors and all three xlsx callers compile unchanged; npm audit reports no HIGH severity advisory for xlsx.</done>
</task>

</tasks>

<verification>
- `package.json`: next=16.2.9, eslint-config-next=16.2.9, xlsx=cdn.sheetjs.com/xlsx-0.20.3 tarball — no other deps changed.
- `npm install` completes; `package-lock.json` regenerated and references the 0.20.3 CDN tarball.
- `npm run build` passes with 0 errors — three xlsx callers compile without source changes.
- `npm audit` shows no HIGH advisory for xlsx.
- Only `package.json` + `package-lock.json` are committed; unrelated dirty working-tree files are left untouched.
</verification>

<success_criteria>
- next no longer at vulnerable 16.2.6 (now 16.2.9, MEDIUM advisory resolved).
- xlsx no longer at vulnerable 0.18.5 (now 0.20.3, both HIGH CVEs CVE-2023-30533 and CVE-2024-22363 resolved).
- Build is green; no caller code changed.
- Commit scope limited to package.json + package-lock.json.
</success_criteria>

<output>
Create `.planning/quick/260611-sz5-fix-dependency-vulnerabilities-next-16-2/260611-sz5-SUMMARY.md` when done.
</output>
